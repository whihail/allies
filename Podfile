platform :ios, '8.0'

target 'multi-ios' do
    
    #  use_frameworks!
    
    pod 'jsbridge-ios', :git => 'git@crt-git.crc.com.cn:app/jsbridge-ios.git'

end

target 'multi-ios-ent' do
    
    #  use_frameworks!
    
    pod 'jsbridge-ios', :git => 'git@crt-git.crc.com.cn:app/jsbridge-ios.git'

end
